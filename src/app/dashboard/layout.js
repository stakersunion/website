export const metadata = {
  title: 'Member Dashboard | Stakers Union',
  description: 'Aggregate statistics and availability signals from verified Stakers Union members.',
}

const DashboardLayout = ({ children }) => {
  return children
}

export default DashboardLayout
